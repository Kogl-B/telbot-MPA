# Content Manager — Локальный менеджер контента

Эта программа работает **только на локальном компьютере**.
Сортирует контент и готовит пакеты для загрузки на сервер.

## Быстрый старт

### 🖥️ Графический интерфейс (рекомендуется)

```bash
cd sorter
python gui.py
```

**Или двойным кликом на файл `gui.py`**

### 💻 Командная строка

```bash
cd sorter
python content_manager.py          # Интерактивное меню
python content_manager.py sort     # Сортировка inbox → storage
python content_manager.py form     # Создание пакета для сервера
python content_manager.py stats    # Статистика по контенту
python content_manager.py upload   # Инструкция по загрузке
```

## Папки

| Папка | Назначение |
|-------|------------|
| `inbox/` | Входящие изображения (сюда кладёте новые файлы) |
| `storage/` | Отсортированный контент по каналам/категориям |
| `upload/` | Готовые пакеты для загрузки на сервер |
| `logs/` | Журналы работы |

## Workflow

1. **Добавьте изображения** в `inbox/` (в папки по именам категорий)
2. **Запустите сортировку** — изображения перейдут в `storage/`
3. **Сформируйте пакет** — готовые файлы появятся в `upload/YYYY-MM/`
4. **Загрузите на сервер** — скопируйте содержимое в `telbot/content/`

## Формат inbox

```
inbox/
├── Azula/          # Изображения будут в storage/atla/Azula/
│   ├── pic1.jpg
│   └── pic2.png
├── Hinata/         # → storage/naruto/Hinata/
├── Hermione Grander/  # → storage/harry_potter/Hermione Grander/
└── Elsa Frozzen/   # → storage/mpa_disney/Elsa Frozzen/
```

## Загрузка на сервер

```bash
# SCP
scp -r upload/2025-01/* user@server:/opt/telbot/content/

# rsync
rsync -avz upload/2025-01/ user@server:/opt/telbot/content/
```
