# 22 — Обработка ошибок и Edge Cases

> **Аннотация:** Промпт для проектирования стратегии обработки ошибок: типы, коды, сообщения, retry-политики, fallback-поведение. Предотвращает молчаливые падения и неинформативные сообщения.

## Когда использовать

- Проектирование нового модуля
- Улучшение существующей обработки ошибок
- Подготовка к продакшену
- После инцидента — предотвращение повторения

---

## Промпт

```
# Роль
Ты — Senior Developer, специализирующийся на надёжности систем.

# Код / Модуль
```
${code}
```

# Контекст
- Это: ${moduleType} (API / UI / background job / data pipeline)
- Критичность: ${criticality} (high / medium / low)
- Пользователи: ${userType} (клиенты / разработчики / система)

# Задача
Спроектируй комплексную обработку ошибок.

# Проанализируй и спроектируй

## 1. Каталог ошибок
Для каждой возможной ошибки:
| Ошибка | Тип | Код | Сообщение (user) | Сообщение (лог) | Действие |
|--------|-----|-----|-------------------|-----------------|----------|

## 2. Стратегия обработки
- Где ловить (на каком уровне)
- Что логировать (контекст, stack trace)
- Что возвращать пользователю
- Что НЕ возвращать (внутренние детали)

## 3. Retry-политика
Для каждого внешнего вызова:
- Retry: да/нет
- Количество попыток
- Backoff стратегия (linear / exponential)
- Таймаут

## 4. Fallback-поведение
Что делать, если:
- БД недоступна
- Внешний API не отвечает
- Кэш пуст
- Диск полон

## 5. Edge Cases
Полный список граничных случаев:
- Пустые данные
- Null/undefined
- Слишком большие данные
- Конкурентные операции
- Таймауты
- Некорректные типы

## 6. Код обработки
Полный код реализации для данного модуля.
```

---

## Советы

- Прикладывайте диаграмму вызовов — ИИ покроет все точки отказа
- Для API: укажите стандарт формата ошибок проекта
- Комбинируйте с `05-bug-detection.md` для поиска непокрытых ошибок

## Роль в агентной системе

Проектирование обработки ошибок — часть этапа планирования модуля (02).
Результат → вход для 🏗️ Implement Agent (03).

**Prompt Chaining**: План модуля (02) → Errors (22) → Implement (03) → Test (12)

## ⚠️ Антипаттерны

- **Assumption Trap**: "Это не упадёт" → проектируй каждый edge case
- **Vagueness**: "Обработай ошибки" → каталог ошибок с кодами, сообщениями, действиями
- **Security Neglect**: Не возвращай внутренние детали пользователю (stack trace, SQL)
