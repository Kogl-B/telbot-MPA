# 04 — Генерация кода

> **Аннотация:** Универсальный промпт для генерации кода любого типа — функции, классы, API-эндпоинты, утилиты. Поддерживает шаблонные переменные для быстрой адаптации под конкретную задачу.

## Когда использовать

- Создание новой функции / класса / модуля с нуля
- Генерация boilerplate-кода
- Быстрое прототипирование

---

## Промпт: Функция

```
Напиши функцию на ${language}, которая ${description}.

Требования:
- Вход: ${inputTypes}
- Выход: ${outputType}
- Граничные случаи: ${edgeCases}
- Производительность: ${performance}

Включи:
- Type hints/аннотации
- Docstring с примерами
- Валидацию входных данных
- Обработку ошибок

Ограничения:
- ${constraints}
```

## Промпт: Класс

```
Создай класс на ${language} для ${purpose}.

Дизайн:
- Имя: ${className}
- Ответственность: ${responsibility}
- Свойства: ${properties}
- Методы: ${methods}

Требования:
- Паттерн: ${designPattern}
- Инкапсуляция, docstrings, пример использования
- Скелет unit-тестов
```

## Промпт: API-эндпоинт

```
Создай REST API эндпоинт для ${resource}.

Фреймворк: ${framework}
Метод: ${method}, Путь: ${path}

Запрос:
- Заголовки: ${headers}
- Тело: ${bodySchema}
- Query-параметры: ${queryParams}

Ответ:
- Успех: ${successResponse}
- Ошибки: ${errorResponses}

Включи: валидацию, аутентификацию, обработку ошибок, rate limiting.
```

## Промпт: React-компонент

```
Создай React-компонент ${componentName}.

Назначение: ${purpose}
Props:
${propsInterface}

Поведение:
- ${behavior1}
- ${behavior2}

Требования:
- Функциональный компонент + hooks
- TypeScript + строгая типизация
- Tailwind CSS для стилей
- Доступность: aria-атрибуты, keyboard navigation
- Responsive: mobile-first
```

---

## Интеграция с агентной системой

Этот промпт = суб-инструмент для **🏗️ Implement Agent** (Sub-Agent Coding Agent'a).
После генерации кода → делегируй 🧪 Test Agent (12) для написания тестов.

## ⚠️ Антипаттерны при генерации

| Антипаттерн | Пример | Решение |
|-------------|--------|--------|
| **Vagueness Trap** | "Напиши функцию" | Укажи вход/выход, edge cases, ограничения |
| **Format Neglect** | Нет указаний на формат | Укажи TS/JS, стиль, паттерн |
| **Trust Everything** | Не проверил сгенерированный код | Прогони линтер, тесты, E2E |
| **Hallucination** | Использует несуществующий API | Приложи документацию / типы библиотеки |

## Продвинутые техники

- **Few-Shot**: Всегда прикладывай пример существующего кода проекта — это лучший способ передать стиль
- **Prompt Chaining**: Для сложных генераций: типы → логика → тесты
- **CoT**: Для алгоритмического кода — сначала "думай пошагово", потом пиши код
- **Verification**: Попроси модель проверить код после генерации: "Теперь проверь код на ошибки и edge cases"

## Советы

- Всегда прикладывайте пример существующего кода проекта (Few-Shot)
- Указывайте конкретные edge cases — иначе ИИ их пропустит
- Не доверяй сгенерированному коду слепо (**Trust Everything Trap**) — проверяй
- Модель может "выдумать" несуществующий API (**Hallucination Trap**) — прикладывай типы
