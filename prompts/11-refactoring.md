# 11 — Рефакторинг кода

> **Аннотация:** Промпт для системного рефакторинга с сохранением поведения. Обнаружение code smells, применение паттернов, улучшение структуры. Код ДО/ПОСЛЕ для каждого изменения.

## Когда использовать

- Код работает, но плохо структурирован
- Подготовка к добавлению нового функционала
- Уменьшение технического долга
- Код стал слишком сложным

---

## Промпт: Общий рефакторинг

```
# Роль
Ты — Senior Developer, специализирующийся на рефакторинге.
Задача: улучшить структуру БЕЗ изменения поведения.

# Код для рефакторинга
```
${code}
```

# Контекст
- Проект: ${projectDescription}
- Этот код: ${codeResponsibility}
- Планы: ${futurePlans}
- Покрытие тестами: ${testCoverage}

# Проанализируй на code smells
1. Длинные методы → предложи извлечение
2. Дублирование → предложи DRY-улучшения
3. Сложные условия → предложи упрощение
4. Плохое именование → предложи лучшие имена
5. Сильная связанность → предложи развязку
6. God Object/Function → предложи разделение
7. Magic numbers/strings → предложи константы/enum
8. Глубокая вложенность → предложи early return / guard clauses

# Правила рефакторинга
- Каждое изменение — один шаг (атомарный коммит)
- Поведение НЕ меняется
- Тесты должны проходить после каждого шага
- Код после рефакторинга < код до (или = при улучшении структуры)

# Формат
Для каждого рефакторинга:
### Шаг N: [название приёма]
**Smell:** [что плохо]
**Приём:** [Extract Method / Rename / Replace Conditional / ...]
**До:**
```
[код]
```
**После:**
```
[код]
```
**Почему лучше:** [1 предложение]
```

## Промпт: Применение паттерна

```
Отрефактори этот код, используя паттерн ${patternName}.

Текущий код:
```
${code}
```

Цели:
- ${whyPattern}
- ${benefits}

Предоставь:
1. Объяснение паттерна (кратко)
2. Как он применяется здесь
3. Отрефакторенный код (полностью)
4. Пошаговый план миграции (если нельзя за 1 шаг)
5. Компромиссы
```

---

## Советы

- Прикладывайте тесты — ИИ убедится, что они пройдут
- Указывайте планы развития — это влияет на выбор паттерна
- Пошаговый рефакторинг безопаснее, чем "большой взрыв"
## Роль в агентной системе

Это **🔧 Refactor Agent** — Sub-Agent Coding Agent'a.
**Prompt Chaining**: Анализ (06) → Рефакторинг (этот) → Тесты (12) → Ревью (07)

## ⚠️ Антипаттерны

- **Overloading**: Не рефактори всё сразу — один приём = один коммит
- **Trust Everything**: После каждого шага — прогоняй тесты
- **Build on Broken**: Не рефактори сломанный код — сначала почини