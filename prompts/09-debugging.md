# 09 — Отладка и исправление багов

> **Аннотация:** Промпт для систематической отладки проблемного кода. Использует Chain of Thought: трассировка → локализация → корневая причина → исправление → предотвращение. Работает как с конкретными ошибками, так и с непредсказуемым поведением.

## Когда использовать

- Код выдаёт ошибку (есть stack trace)
- Код работает, но результат неправильный
- Непредсказуемое / периодическое поведение
- Регрессия после изменений

---

## Промпт: С ошибкой

```
# Роль
Ты — Senior Debugger. Используй Chain of Thought: думай шаг за шагом.

# Проблемный код
Файл: ${filePath}
```
${code}
```

# Ошибка
```
${errorMessage}
```

# Контекст
- Ожидаемое поведение: ${expectedBehavior}
- Фактическое поведение: ${actualBehavior}
- Когда возникает: ${whenOccurs}
- Воспроизводимость: ${reproducibility} (всегда / иногда / редко)
- Что пробовали: ${triedSolutions}

# Шаги отладки (думай вслух)
1. Определи, что код пытается сделать
2. Проследи выполнение строка за строкой с конкретными значениями
3. Найди точку, где ожидаемое расходится с фактическим
4. Объясни корневую причину
5. Предоставь исправление

# Формат вывода
## Трассировка
[пошаговое выполнение с конкретными значениями]

## Корневая причина
[почему это происходит]

## Исправление
[код]

## Почему это работает
[объяснение]

## Как предотвратить
[что добавить, чтобы это не повторилось]
```

## Промпт: Без ошибки (неправильное поведение)

```
# Роль
Ты — Senior Debugger. Код не выдаёт ошибку, но ведёт себя неправильно.

# Код
```
${code}
```

# Ожидалось: ${expected}
# Получено: ${actual}
# Тестовые данные: ${testData}

Проследи выполнение кода шаг за шагом с данными выше.
На каждом шаге покажи значения всех переменных.
Найди, где результат начинает расходиться с ожидаемым.
Объясни почему и дай исправление.
```

## Промпт: Интерпретация stack trace

```
Объясни эту ошибку и как её исправить:

```
${stackTrace}
```

Контекст:
- Фреймворк: ${framework}
- Что я делал: ${action}
- Код: ${codeSnippet}

Предоставь:
1. Объяснение ошибки простым языком
2. Корневую причину
3. Пошаговое исправление
4. Как предотвратить в будущем
```

---

## Роль в агентной системе

Это **🐛 Debug Agent** — Sub-Agent Coding Agent'a.
Вызывается в рамках принципа **FIX-FIRST**: если при Boot Sequence обнаружены проблемы — сначала Debug Agent, потом новые фичи.

**Prompt Chaining отладки:**
```
🐛 Debug (CoT) → 🛠️ Fix → 🧪 Test (12) → 👀 Review (07) → ✅ Commit
```

## Продвинутые техники отладки

### Chain of Thought — BREAK-формат
```
GIVEN: [известные факты]
GOAL: [желаемое поведение]
APPROACH: [стратегия]
STEPS:
  1. [трассировка + значения]
  2. [находка расхождения]
CHECK: [верификация]
ANSWER: [исправление + предотвращение]
```

### Self-Consistency: реши баг 2-3 способами, сравни
### E2E: Puppeteer MCP для UI-багов — как реальный пользователь

## ⚠️ Антипаттерны

| Антипаттерн | Решение |
|-------------|--------|
| **Build on Broken** | FIX-FIRST всегда |
| **Trust Everything** | E2E верификация |
| **One-Shot** | Итерируй: fix → test → fix |
| **Assumption** | Трассировка с значениями |

## Советы

- Прикладывайте тестовые данные — трассировка без данных бесполезна
- После фикса: добавьте регрессионный тест
- Обновите progress.txt и сделайте `fix:` коммит
