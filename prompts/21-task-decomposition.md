# 21 — Декомпозиция задач

> **Аннотация:** Промпт для трёхуровневой декомпозиции любой задачи: L1 эпики → L2 модули → L3 атомарные шаги. Каждый L3-шаг выполним за одну ИИ-сессию.

## Когда использовать

- Получили крупную задачу — нужно разбить
- Планирование спринта
- Оценка трудозатрат
- Создание тасков в трекере

---

## Промпт

```
# Роль
Ты — Project Manager с техническим бэкграундом.

# Задача для декомпозиции
${taskDescription}

# Контекст проекта
- Стек: ${techStack}
- Текущее состояние: ${currentState}
- Зависимости: ${dependencies}

# Правила декомпозиции

## L1 — Эпики
- Крупные блоки функциональности
- Понятные бизнесу
- Не более 7

## L2 — Модули/Фичи
- Самостоятельные единицы работы
- Имеют чёткий Definition of Done
- Каждый — отдельный PR

## L3 — Атомарные шаги
- Выполнимы за 1-2 часа (одна ИИ-сессия)
- Оставляют проект в рабочем состоянии
- Имеют конкретный результат (файлы, функции, тесты)

# Стратегии выполнения
Для каждого L2 укажи:
- **Sequential** — нужен порядок
- **Parallel** — можно параллельно
- **Iterative** — цикл доработки

# Формат
```
L1: [Название эпика] (P0/P1/P2)
  ├── L2: [Модуль/Фича] [S/M/L/XL] [Sequential/Parallel]
  │   ├── L3: [Атомарный шаг] → [результат]
  │   ├── L3: [Атомарный шаг] → [результат]
  │   └── L3: [Атомарный шаг] → [результат]
  └── L2: ...
```

В конце:
- Общее количество L3-задач
- Оценка общего времени
- Критический путь
- Что можно параллелить
```

---

## Советы

- Каждый L3 должен быть формулировкой для Coding Agent
- Проверяйте: может ли L3-шаг быть выполнен без контекста предыдущих шагов?
- Если L3 больше 2 часов — разбейте ещё

## Выход → features.json

Каждая L3-задача = одна запись в features.json:
```json
{
  "id": "feat-XXX",
  "category": "functional",
  "description": "[из L3: конкретное описание]",
  "priority": "P0",
  "steps": ["Navigate to...", "Click...", "Verify..."],
  "passes": false
}
```

## Делегирование в агентной системе

Каждая L3-задача передаётся Coding Agent (20), который может делегировать Sub-Agents:
```
L3-задача → Coding Agent (20)
  ├── 🏗️ Implement Agent (03/04) — реализация
  ├── 🧪 Test Agent (12) — тесты
  ├── 👀 Review Agent (07) — ревью
  └── features.json: passes: false → true
```

## ⚠️ Антипаттерны декомпозиции

| Антипаттерн | Пример | Решение |
|-------------|--------|--------|
| **Vagueness** | L3: "Сделать настройки" | L3: "Создать SettingsPage с формой profile + тема" |
| **Overloading** | L3 на 4 часа | Разбить на 2 L3 по 2 часа |
| **Assumption** | L3 зависит от предыдущего контекста | Каждый L3 самодостаточен |

## Продвинутые техники

- **Prompt Chaining**: Сначала декомпозируй L1, затем каждый L1 → L2 отдельным промптом
- **Self-Consistency**: Попроси 2 варианта разбивки, сравни
- **CoT**: "Думай пошагово: какие зависимости? что можно параллелить? где риски?"
