# 12 — Генерация тестов

> **Аннотация:** Промпт для создания полных наборов тестов: unit, интеграционные, E2E. Покрывает happy path, граничные случаи, ошибки. Формат Arrange-Act-Assert.

## Когда использовать

- Написание тестов для новой функциональности
- Покрытие существующего кода тестами
- Подготовка тест-кейсов для QA
- После рефакторинга — страховочная сеть

---

## Промпт: Unit-тесты

```
# Роль
Ты — Senior QA Engineer, пишущий тесты на ${testFramework}.

# Код для тестирования
```
${code}
```

# Задача
Напиши ВСЕ необходимые unit-тесты.

# Покрой:
1. **Happy path** — нормальные входные данные (3+ сценариев)
2. **Граничные случаи** — min, max, пустые, null, 0, 1, большие данные
3. **Ошибки** — невалидные данные, исключения
4. **Типы** — неправильные типы, NaN, Infinity
5. **Конкурентность** — если применимо

# Требования
- Формат: Arrange → Act → Assert
- Описательные имена: should_returnX_when_Y
- Каждый тест — один сценарий
- Моки для внешних зависимостей
- Нет зависимости между тестами
- Тесты должны быть быстрыми (< 100мс каждый)

# Формат
```${language}
describe('${functionName}', () => {
  // Happy path
  it('should ... when ...', () => {
    // Arrange
    // Act
    // Assert
  });

  // Edge cases
  // Error cases
});
```
```

## Промпт: Тест-кейсы (таблица)

```
Сгенерируй тест-кейсы для: ${featureDescription}
Критерии приёмки: ${acceptanceCriteria}

Формат:

| ID | Сценарий | Предусловия | Действие | Ожидаемый результат | Приоритет |
|----|----------|-------------|----------|---------------------|-----------|
| TC-001 | ... | ... | ... | ... | High/Medium/Low |
```

## Промпт: E2E-тесты

```
Напиши E2E-тесты для: ${userFlow}
Фреймворк: ${e2eFramework} (Playwright / Cypress / Puppeteer)

Покрой полный пользовательский путь:
1. ${step1}
2. ${step2}
3. ${step3}

Для каждого шага:
- Действие пользователя
- Ожидаемый результат
- Что проверить (assertions)
- Скриншот-точки (если нужно)
```

---

## Советы

- Подавайте тестируемый код целиком — ИИ покроет все ветки
- Для сложных функций: попросите `таблицу входов → выходов` перед тестами
- Укажите конвенции именования тестов вашего проекта

## Роль в агентной системе

Это **🧪 Test Agent** — Sub-Agent Coding Agent'a.

### JSON-трекинг фич и тесты
Каждая фича в features.json имеет `steps` — это E2E-сценарии.
Test Agent должен покрыть ВСЕ steps тестами.

```json
// features.json — источник тест-кейсов:
{
  "id": "feat-003",
  "steps": [
    "Navigate to settings",      // → E2E тест
    "Toggle dark theme",          // → E2E тест
    "Verify all components switch" // → E2E тест
  ],
  "passes": false  // → true только после прохождения ВСЕХ steps
}
```

### ⛔ Запрет: нельзя удалять или менять steps в features.json
Можно только: `passes: false → true` после реальной проверки.

### E2E через Puppeteer MCP
Для UI-тестов: Claude запускает локальный сервер, открывает страницу,
кликает кнопки, проверяет ответы — как реальный пользователь.

**Без E2E агенты отмечают фичи как done, даже если они не работают.**

## ⚠️ Антипаттерны тестирования

| Антипаттерн | Описание | Решение |
|-------------|----------|--------|
| **Trust Everything** | passes=true без реальной проверки | Всегда E2E |
| **One-Shot** | Тесты только на happy path | Edge cases + errors |
| **Vagueness** | "Проверить что работает" | Конкретные assert'ы |

## Prompt Chaining

```
🏗️ Implement (03) → 🧪 Test (этот) → 👀 Review (07) → ✅ passes: true
```
