# 03 — Реализация фичи

> **Аннотация:** Промпт для непосредственной реализации конкретной фичи. ИИ получает спецификацию и производит готовый, протестированный код, следуя конвенциям проекта.

## Когда использовать

- Реализация конкретной задачи из плана разработки
- Добавление нового функционала в существующий модуль
- Кодинг по чёткой спецификации

---

## Промпт

```
# Роль
Ты — Senior ${language} Developer, работающий над проектом ${projectName}.

# Контекст проекта
Стек: ${techStack}
Конвенции:
- Именование: ${namingConventions}
- Структура: ${fileStructure}
- Паттерны: ${patterns}

# Текущий код (релевантные файлы)
${existingCode}

# Задача
Реализуй фичу: ${featureDescription}

# Требования
1. ${requirement1}
2. ${requirement2}
3. ${requirement3}

# Ограничения
- Не добавляй новые зависимости
- Не изменяй существующие интерфейсы/API-контракты
- Следуй паттернам из существующего кода
- Код должен компилироваться и проходить линтер
- Оставь проект в рабочем состоянии

# Формат вывода
Для каждого файла:
1. Путь к файлу
2. Полный код (не фрагменты)
3. Краткое объяснение (1-2 предложения)

В конце:
- Команды для запуска/проверки
- Что протестировать вручную
- Обновление для progress.txt
```

---

## Ожидаемый результат

- Готовый код всех затронутых файлов
- Инструкции по проверке
- Обновление трекинга прогресса

## Интеграция с агентной системой

Этот промпт используется как **🏗️ Implement Agent** — Sub-Agent для Coding Agent (20).

### Выполнив реализацию:
1. Проверь все steps из features.json для данной фичи
2. Если все шаги проходят → `passes: true`
3. Обнови progress.txt
4. Сделай коммит: `feat: ${featureId} - ${description}`

### JSON-трекинг фич:
```json
// После успешной реализации и E2E-проверки:
{
  "id": "feat-005",
  "passes": true,  // было false
  "assigned_session": "session-3",
  "completed_at": "2026-02-27T14:30:00Z"
}
// ⛔ NЕЛЬЗЯ менять steps или description!
```

## ⚠️ Антипаттерны реализации

| Антипаттерн | Симптом | Решение |
|-------------|---------|--------|
| **Trust Everything** | Отметил passes=true без проверки | E2E верификация каждого step |
| **Assumption Trap** | "Наверное там такой API" | Прочитай существующий код перед реализацией |
| **One-Shot Trap** | Первый код = финальный | Итерируй: код → тест → fix |
| **Security Neglect** | Секреты в коде | .env для секретов, никогда hardcode |

## Продвинутые техники

- **Prompt Chaining**: Сложные фичи разбивай на цепочку: Типы → Интерфейсы → Логика → Тесты → Интеграция
- **Few-Shot**: Всегда прикладывай аналогичный модуль как образец стиля
- **CoT**: Для сложной логики — "Think step by step" перед написанием кода
- **Sub-Agents**: После реализации делегируй 🧪 Test Agent (12) и 👀 Review Agent (07)

## Советы

- Всегда прикладывайте существующий код аналогичных модулей (Few-Shot)
- Указывайте конкретные ограничения (не более 200 строк, async/await)
- Не отмечайте фичу как done без E2E-проверки всех steps
