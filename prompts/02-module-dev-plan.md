# 02 — План разработки модуля

> **Аннотация:** Промпт для детального планирования одного модуля/фичи программы. Создаёт пошаговый план реализации с учётом существующей архитектуры, зависимостей и конвенций проекта.

## Когда использовать

- Начало работы над новым модулем / фичей
- Планирование крупного изменения в существующем модуле
- Перед делегированием задачи ИИ-агенту

---

## Промпт

```
# Роль
Ты — Senior Software Architect, специализирующийся на ${techStack}.

# Контекст проекта
Проект: ${projectName}
Стек: ${techStack}
Архитектура: ${architecture}
Текущая структура файлов:
${fileStructure}

# Модуль для разработки
Название: ${moduleName}
Описание: ${moduleDescription}
Связанные модули: ${relatedModules}

# Задача
Создай детальный план разработки модуля.

# Что включить
1. **Цель модуля** — что он делает, какую проблему решает
2. **Интерфейсы** — входы, выходы, API-контракты
3. **Зависимости** — от каких модулей зависит, какие модули зависят от него
4. **Структура файлов** — какие файлы создать/изменить
5. **Пошаговый план** — последовательность реализации (каждый шаг = 1 коммит)
6. **Модель данных** — если нужна (таблицы, схемы, типы)
7. **Тесты** — что тестировать, какие сценарии покрыть
8. **Edge cases** — граничные случаи и как их обрабатывать

# Ограничения
- Следуй конвенциям проекта: ${conventions}
- Не добавляй зависимости без обоснования
- Каждый шаг должен оставлять проект в рабочем состоянии
- Plan for failure — каждый внешний вызов должен иметь fallback

# Формат
Пошаговый план в Markdown:
1. Шаг → описание → файлы → критерий готовности
2. ...

В конце: JSON-массив фич для tests.json.
```

---

## Ожидаемый результат

- Пошаговый план из 5-15 шагов
- Структура файлов
- Интерфейсы и типы данных
- JSON-записи для features.json

## Выход → features.json

Каждый шаг плана должен генерировать записи для features.json:
```json
{
  "id": "feat-${modulePrefix}-001",
  "category": "${category}",
  "description": "Конкретное описание фичи",
  "priority": "P0",
  "steps": [
    "Navigate to [page]",
    "Perform [action]",
    "Verify [expected result]"
  ],
  "passes": false,
  "assigned_session": null,
  "completed_at": null
}
```

## Делегирование Sub-Agents

Каждый шаг плана можно делегировать специализированному агенту:
```
Шаг 1: Создать типы → 🏗️ Implement Agent (04-code-generation.md)
Шаг 2: Реализовать логику → 🏗️ Implement Agent (03-feature-implementation.md)
Шаг 3: Написать тесты → 🧪 Test Agent (12-testing.md)
Шаг 4: Ревью → 👀 Review Agent (07-code-review.md)
```

## ⚠️ Антипаттерны

- **Overloading Trap**: Не планируй больше 15 шагов — разбей на 2 модуля
- **Assumption Trap**: Не предполагай интерфейсы — приложи существующие файлы
- **Format Neglect**: Всегда завершай JSON-заготовками для features.json

## Советы

- Прикладывайте существующие файлы аналогичных модулей как **Few-Shot** пример
- Результат можно напрямую передать Coding Agent (20)
- Используйте **Prompt Chaining**: сначала интерфейсы → потом план реализации
